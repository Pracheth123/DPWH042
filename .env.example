# ── GhostGrid Runtime Configuration ──────────────────────────────────────────

APP_NAME=GhostGrid
DEBUG=false

# Alert query defaults
ALERT_LOOKBACK_HOURS=72
MAX_ALERTS_RETURNED=50

# ── NLP Team: fill these in when the GPU model is ready ───────────────────────
NLP_MODEL_ENDPOINT=
NLP_API_KEY=

# ── Supabase: fill these in when DB migration happens ─────────────────────────
SUPABASE_URL=
SUPABASE_KEY=
